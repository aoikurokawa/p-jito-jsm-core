import React from 'react';

const Balance = ({account}) => {
    return (
        <div>
            <h1>Coin component</h1>
            <p>account: {account}</p>
        </div>
    );
}

export default Balance;
